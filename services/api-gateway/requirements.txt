# ---------------------------------------------------------------------------
# OmniWatch — API Gateway Python Dependencies
# Phase: 1
# ---------------------------------------------------------------------------

# -- Web framework --
fastapi>=0.110.0

# -- ASGI server --
uvicorn[standard]>=0.29.0

# -- OpenTelemetry --
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
opentelemetry-exporter-otlp-proto-grpc>=1.25.0

# -- Data validation --
pydantic>=2.7.0

# -- Async HTTP client (proxy to downstream services) --
httpx>=0.27.0
