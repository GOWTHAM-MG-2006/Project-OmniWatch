# =============================================================================
# OmniWatch — Identity Service (entry-point, Wave 1)
# =============================================================================
#
# Runtime dependencies for the FastAPI register/login/JWT service.
# Lower bounds match repo style (learning/predictive requirements.txt).
# Test runner (pytest, pytest-asyncio) is pinned in the repo-root
# requirements.txt — not duplicated here.
# =============================================================================

# -- API framework -------------------------------------------------------------
fastapi>=0.109.0                    # FastAPI app (main.py, auth.py routers)
uvicorn>=0.27.0                     # ASGI server (main.py CLI)

# -- Storage client ------------------------------------------------------------
clickhouse-connect>=0.7.0           # ClickHouse HTTP client (store.ClickHouseStore)

# -- Auth ----------------------------------------------------------------------
bcrypt>=4.0.0                       # Password hashing (auth.py; never plaintext)
pyjwt>=2.8.0                        # HS256 JWT mint/verify (tokens.py)

# -- Configuration / validation ------------------------------------------------
pydantic>=2.5.0                     # Data validation (models.py)
pydantic-settings>=2.1.0            # Env-var loading (settings.py BaseSettings)

# -- HTTP client (tests + health probes) ---------------------------------------
httpx>=0.27.0                       # TestClient transport (identity/tests/)
