# ---------------------------------------------------------------------------
# OmniWatch — Root Python Dependencies
# Phase: 5 (Unified Storage Layer)
# ---------------------------------------------------------------------------

# -- Unified Storage Layer (Phase 5) --
clickhouse-connect>=1.4.2
neo4j>=6.2.0
minio>=7.2.20

# -- Testing --
pytest>=9.0.0
pytest-asyncio>=1.3.0
