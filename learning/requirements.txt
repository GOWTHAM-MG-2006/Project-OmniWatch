# =============================================================================
# OmniWatch — Continuous Learning Layer (Phase 11)
# =============================================================================
#
# Runtime dependencies for the Learning Service FastAPI service.
# Version pins match orchestration/predictive requirements.txt.
# SKLEARN SPLIT (DYN-python): this service pins NO numpy/scikit-learn of its
# own — it must not pull either the causal (numpy==1.23.5/sklearn<1.2) or the
# predictive (numpy>=1.26/sklearn>=1.5) set. Never install both sets here.
# =============================================================================

# -- API framework -------------------------------------------------------------
fastapi>=0.109.0                    # FastAPI app (learning_service.py)
uvicorn[standard]>=0.27.0          # ASGI server (learning_service.py CLI)

# -- Kafka (FeedbackLoopProcessor) ---------------------------------------------
confluent-kafka>=2.3.0              # KafkaConsumer (feedback_loop.py)

# -- Storage clients -----------------------------------------------------------
clickhouse-connect>=0.7.0           # ClickHouse HTTP client (feedback_loop, pattern_mining, recommendation_engine)
neo4j>=5.17.0                       # Neo4j Bolt driver (pattern_mining.py)

# -- Configuration / validation ------------------------------------------------
pydantic>=2.5.0                     # Data validation

# -- Kubernetes ----------------------------------------------------------------
kubernetes>=28.0.0                  # K8s Lease leader election (common/leaderelection.py);
                                    # optional at runtime — missing client falls back to standalone
