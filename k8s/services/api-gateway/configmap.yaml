apiVersion: v1
kind: ConfigMap
metadata:
  name: api-gateway-config
  namespace: omniwatch
  labels:
    app: api-gateway
    tier: backend
    phase: "1"
data:
  KAFKA_BOOTSTRAP_SERVERS: kafka:9092
  SERVICE_NAME: api-gateway
