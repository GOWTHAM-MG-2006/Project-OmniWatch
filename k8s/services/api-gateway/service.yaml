apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: omniwatch
  labels:
    app: api-gateway
    tier: backend
    phase: "1"
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app: api-gateway
