apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: omniwatch
  labels:
    app: user-service
    tier: backend
    phase: "1"
spec:
  type: ClusterIP
  ports:
    - port: 8001
      targetPort: 8001
      name: http
  selector:
    app: user-service
