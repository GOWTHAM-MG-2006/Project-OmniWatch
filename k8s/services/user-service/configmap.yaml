apiVersion: v1
kind: ConfigMap
metadata:
  name: user-service-config
  namespace: omniwatch
  labels:
    app: user-service
    tier: backend
    phase: "1"
data:
  KAFKA_BOOTSTRAP_SERVERS: kafka:9092
