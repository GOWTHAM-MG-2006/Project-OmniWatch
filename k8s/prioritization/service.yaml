apiVersion: v1
kind: Service
metadata:
  name: prioritization
  namespace: omniwatch
  labels:
    app: prioritization
    tier: backend
    phase: "8"
spec:
  type: ClusterIP
  ports:
    - port: 8009
      targetPort: 8009
      name: http
  selector:
    app: prioritization
