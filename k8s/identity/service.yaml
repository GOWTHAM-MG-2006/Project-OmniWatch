apiVersion: v1
kind: Service
metadata:
  name: identity
  namespace: omniwatch
  labels:
    app: identity
    tier: backend
    phase: "entry-point"
spec:
  type: ClusterIP
  ports:
    - port: 8012
      targetPort: 8012
      name: http
  selector:
    app: identity
