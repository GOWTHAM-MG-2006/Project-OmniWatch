apiVersion: v1
kind: Service
metadata:
  name: ollama
  namespace: omniwatch
  labels:
    app.kubernetes.io/name: ollama
    app.kubernetes.io/part-of: omniwatch
spec:
  ports:
    - port: 11434
      targetPort: 11434
      name: http
  selector:
    app.kubernetes.io/name: ollama
