apiVersion: v1
kind: Service
metadata:
  name: clickhouse
  namespace: omniwatch
  labels:
    app.kubernetes.io/name: clickhouse
    app.kubernetes.io/part-of: omniwatch
spec:
  ports:
    - port: 9000
      targetPort: 9000
      name: native
    - port: 8123
      targetPort: 8123
      name: http
  selector:
    app.kubernetes.io/name: clickhouse
