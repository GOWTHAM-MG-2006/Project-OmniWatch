apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: omniwatch
  labels:
    app.kubernetes.io/name: redis
    app.kubernetes.io/part-of: omniwatch
spec:
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
  selector:
    app.kubernetes.io/name: redis
