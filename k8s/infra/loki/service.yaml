apiVersion: v1
kind: Service
metadata:
  name: loki
  namespace: omniwatch
  labels:
    app.kubernetes.io/name: loki
    app.kubernetes.io/part-of: omniwatch
spec:
  ports:
    - port: 3100
      targetPort: 3100
      name: http
  selector:
    app.kubernetes.io/name: loki
