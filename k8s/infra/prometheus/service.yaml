apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: omniwatch
  labels:
    app.kubernetes.io/name: prometheus
    app.kubernetes.io/part-of: omniwatch
spec:
  ports:
    - port: 9090
      targetPort: 9090
      name: web
  selector:
    app.kubernetes.io/name: prometheus
